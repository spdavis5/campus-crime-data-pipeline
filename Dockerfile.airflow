# Airflow image: the official Airflow plus the Docker provider, so the DAG can
# orchestrate the pipeline's existing job containers via DockerOperator.
FROM apache/airflow:2.10.4

RUN pip install --no-cache-dir apache-airflow-providers-docker==3.14.1
