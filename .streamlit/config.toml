# Pins a consistent light theme for the dashboard so its appearance is predictable
# regardless of the viewer's browser/OS setting. Palette matches the charts.
[theme]
base = "light"
primaryColor = "#2a78d6"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f5f5f3"
textColor = "#0b0b0b"
